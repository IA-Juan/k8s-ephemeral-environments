
apiVersion: v1
kind: ConfigMap

metadata:
  name: {{ .Release.Name }}-postgres-init

data:

  schema.sql: |
{{ .Files.Get "files/schema.sql" | indent 4 }}

  seed.sql: |
{{ .Files.Get "files/seed.sql" | indent 4 }}